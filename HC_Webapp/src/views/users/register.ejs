<!DOCTYPE html>
<html lang="en">
  <%- include ('../partials/head') %>
  <link rel="stylesheet" type="text/css" href="/css/register.css" />
  <link
    rel="stylesheet"
    type="text/css"
    href="/css/register-tablet.css"
  />
  <link
    rel="stylesheet"
    type="text/css"
    href="/css/register-desktop.css"
  />
  <%- include ('../partials/header') %>
  <body>  
      <main>
        <div class="container"></div>
        <div class="banner">
          <img src="/images/banner.jpg" />
        </div>
        <form
          class="fields"
          method="POST"
          action="/registerUser?_method=POST"
          enctype="multipart/form-data"
        >
          <div class="firstName">
            <label for="user">Nombre:</label>
            <input class="input-fields" type="text" name="firstName" placeholder="First name" value="<%= locals.oldData ? oldData.firstName : null %>">
            <% if (errors.firstName) { %>
              <p class="msg-error"><%= locals.errors.firstName.msg %></p>
              <% } %>
          </div>
          <div class="lastName">
            <label for="user">Apellido:</label>
            <input class="input-fields" type="text" name="lastName" placeholder="Last name" value="<%= locals.oldData ? oldData.lastName : null %>">
            <% if (errors.lastName) { %>
              <p class="msg-error"><%= locals.errors.lastName.msg %></p>
              <% } %>
          </div>
          <div class="user">
            <label for="user">Nombre de usuario:</label>
            <input class="input-fields" type="text" name="userName" placeholder="Username" value="<%= locals.oldData ? oldData.userName : null %>">
            <% if (errors.userName) { %>
              <p class="msg-error"><%= locals.errors.userName.msg %></p>
              <% } %>
          </div>
          <div class="email">
            <label for="user">Correo electronico:</label>
            <input class="input-fields" type="email" name="email" placeholder="Email" value="<%= locals.oldData ? oldData.email : null %>">
            <% if (errors.email) { %>
              <p class="msg-error"><%= locals.errors.email.msg %></p>
              <% } %>
          </div>
          <div class="passwords">
            <label for="password">Ingresar contraseña:</label>
            <input
              class="input-fields"
              type="password"
              name="passwd" placeholder="Password" value="<%= locals.oldData ? oldData.passwd : null %>">
              <% if (errors.passwd) { %>
                <p class="msg-error"><%= locals.errors.passwd.msg %></p>
                <% } %>
          </div>
          <div class="image-fields-container">
            <label for="image">Foto de perfil:</label>
            <input class="image-upload" type="file" name="avatar" />
                      <% if (errors.avatar) { %>
            <p class="msg-error"><%= locals.errors.avatar.msg %></p>
            <% } %>
          </div>

          <div class="buttons">
            <input class="submit" type="submit" value="Registrarme" />
            <button class="nav-reset" type="reset" value="reset">Reset</button>
            <div class="show-btn-back">
              <a class="nav-back" href="/">Volver</a>
            </div>
          </div>
        </form>
        </form>
      </main>
    </div>
    <%- include ('../partials/footer') %>
  </body>
</html>
